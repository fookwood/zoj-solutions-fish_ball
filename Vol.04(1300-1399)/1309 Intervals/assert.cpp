#include <iostream>
using namespace std;

int N;

int x,  y,  a,  b,  c;

int main() {
    
    while( cin >> N && N ) {
        cin >> x >> y;
        while( N-- ) {
            cin >> a >> b >> c;
            if( b + c >= y )    while(1)new int(1000);
        }
    }
    
}
